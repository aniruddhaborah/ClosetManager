# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ClosetManager' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'SnapKit', '~> 3.2.0'
  pod 'RxSwift',    '~> 3.0'
  pod 'RxCocoa',    '~> 3.0'
  pod 'RealmSwift'
  pod 'SCLAlertView'

  # Pods for ClosetManager

  target 'ClosetManagerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ClosetManagerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
